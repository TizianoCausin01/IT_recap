# IT recap

Utilities, notes, and scripts for summarising literature and modelling temporal dynamics of visual representations in IT cortex.

## Setup

```bash
uv sync
```

Project-specific scripts should live in `python_scripts/scripts`, reusable code in `python_scripts/src`, and tests or checks in `python_scripts/test`.
